# TideMark
# ========
#
# File: .github/workflows/release-please.yml
# Description: Optional manual semantic-release helper workflow using release-please.
#
# Responsibility:
# - Provide an optional release PR/tag path when maintainers want release-please semantics.
#
# Architectural Position:
# - Release orchestration entrypoint for automated version management.
#
# Author: Silan.Hu
# Email: silan.hu@u.nus.edu
# Copyright (c) 2026-2027 easynet. All rights reserved.

name: release-please

on:
  workflow_dispatch:
    inputs:
      release_as:
        description: "Force next release version (for example: 0.1.0)"
        required: false
        type: string

permissions:
  contents: write
  pull-requests: write

jobs:
  release-please:
    runs-on: ubuntu-latest
    steps:
      - name: Run release-please
        id: release
        uses: googleapis/release-please-action@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          config-file: release-please-config.json
          manifest-file: .release-please-manifest.json
          release-as: ${{ inputs.release_as }}
